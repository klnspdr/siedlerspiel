{
  "name": "siedlerspiel",
  "version": "1.0.0",
  "description": "Siedlerspiel game backend – Node.js + SQLite port",
  "main": "server.js",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "PORT=80 node server.js",
    "start-dev": "nodemon server.js",
    "test": "node test/test.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.6",
    "express": "^4.18.3",
    "express-static": "^1.2.6"
  },
  "notes": {
    "sqlite": "Uses the built-in node:sqlite module (Node >= 22). No extra sqlite dependency needed."
  }
}
